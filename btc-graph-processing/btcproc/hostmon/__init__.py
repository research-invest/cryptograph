"""
Монитор ресурсов хоста: CPU, память, swap, диск, процессы, контейнеры.

Три части:
  `collect` — снимок состояния машины (psutil + `docker stats`);
  `store`   — SQLite-хранилище замеров и выборка рядов для графиков;
  `daemon`  — процесс-сэмплер, пишущий снимок раз в `HOSTMON_INTERVAL_SECONDS`.

Читает всё это страница `/server` админки. Смотреть в БД проекта монитор не
может по построению: он обязан работать и когда лежит docker-стек с Postgres —
см. docstring `config.HostmonConfig`.
"""
